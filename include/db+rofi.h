#ifndef DB_PLUS_ROFI_H
#define DB_PLUS_ROFI_H

#include "db.h"
#include "externc.h"

EXTERN bool db_rofi_is_run_alt (char *cmd, void *closure);

EXTERN bool db_rofi_toggle_run_alt (char *cmd, void *closure);

#endif /* DB_PLUS_ROFI_H */
